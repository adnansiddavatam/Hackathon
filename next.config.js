/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.dicebear.com'], // Add the domain of the external image source
    },
  }
  
  module.exports = nextConfig;
  